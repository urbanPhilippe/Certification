{% extends 'layout.html.twig' %}

{% block title %}Créer un élément de checklist{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card w-100 m-auto">
                    <div class="card-header text-center">
                        <h1>Créer un nouvel élément de checklist</h1>
                    </div>
                    <div class="m-4">
                        {{ include('checklist_item/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-md-center mt-3 mb-5">
        <a class="btn btn-primary" href="{{ path('checklist_item_index') }}">Retour</a>
    </div>

{% endblock %}
