{% extends 'layout.html.twig' %}

{% block title %}Modifier la checklist{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header text-center">
                        <h1>Modifier la checklist</h1>
                    </div>
                    <div class="m-3">
                        {{ include('checklist_item/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                        {{ include('checklist_item/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-center mt-3 mb-5">
        <a class="btn btn-primary" href="{{ path('checklist_item_index') }}">Retour</a>
    </div>

{% endblock %}
