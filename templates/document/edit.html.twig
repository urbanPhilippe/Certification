{% extends 'layout.html.twig' %}

{% block title %}Modifier Document{% endblock %}

{% block body %}

    <div class="container">
        <h1>Modifier le document</h1>

        {{ include('document/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        {{ include('document/_delete_form.html.twig') }}

        <a class="btn btn-primary" href="{{ path('document_index') }}">Retour</a>
    </div>

{% endblock %}
