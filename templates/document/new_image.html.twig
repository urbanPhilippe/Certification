{% extends 'layout.html.twig' %}

{% block title %}Nouveau document{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h1>Ajouter une image</h1>
                        <p>Type acceptés : JPEG, JPG, PNG, GIF (taille max: 2 Mo)</p>

                        <hr>

                        {{ include('document/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-center mb-5">
        <a class="btn btn-primary" href="{{ path('document_index_images') }}">Retour</a>
    </div>

{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
    {{ encore_entry_script_tags('inputDocument') }}
{% endblock %}

