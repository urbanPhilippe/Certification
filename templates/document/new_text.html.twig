{% extends 'layout.html.twig' %}

{% block title %}Nouveau document{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h1>Ajouter un document</h1>

                        <p>Type acceptés : PDF, DOC, DOCX (taille max: 2 Mo)</p>
                        <hr>

                        {{ include('document/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-center mb-5">
        <a class="btn btn-primary" href="{{ path('document_index_texts') }}">Retour</a>
    </div>

{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
    {{ encore_entry_script_tags('inputDocument') }}
{% endblock %}

