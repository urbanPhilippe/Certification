<!-- Modal manager -->
<div class="modal fade" id="modal-manager" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle"
     aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Votre manager</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Manager : <b>{{ app.user.manager ? app.user.manager.firstname ~ ' ' ~ app.user.manager.lastname : 'Non défini'}}</b></p>
                <p>Email : <b>{{ app.user.manager ? app.user.manager.email : ''}}</b></p>
                <p>Téléphone : <b>{{ app.user.manager ? app.user.manager.telephone : ''}}</b></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal referent -->
<div class="modal fade" id="modal-referent" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle"
     aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Votre référent métier</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Référent métier :<b>{{ app.user.referent ? app.user.referent.firstname ~ ' ' ~ app.user.referent.lastname : 'Non défini'}}</b></p>
                <p>Email : <b>{{ app.user.referent ? app.user.referent.email : ''}}</b></p>
                <p>Téléphone : <b>{{ app.user.referent ? app.user.referent.telephone : ''}}</b></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal referent -->
<div class="modal fade" id="modal-mentor" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle"
     aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Votre parrain</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Mentor :<b>{{ app.user.mentor ? app.user.mentor.firstname ~ ' ' ~ app.user.mentor.lastname : 'Non défini' }}</b></p>
                <p>Email : <b>{{ app.user.mentor ? app.user.mentor.email : ''}}</b></p>
                <p>Téléphone : <b>{{ app.user.mentor ? app.user.mentor.telephone : ''}}</b></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
