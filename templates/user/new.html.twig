{% extends 'layout.html.twig' %}

{% block title %}Créer un nouvel utilisateur{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card w-100 m-auto">
                    <div class="card-header text-center">
                        <h1>Créer un nouvel utilisateur</h1>
                    </div>
                    <div class="m-3">
                    {{ include('user/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
        <div class="d-flex justify-content-md-center mt-4 mb-5">
                <a class="btn btn-primary" href="{{ path('user_index') }}">Retour</a>
        </div>
{% endblock %}
