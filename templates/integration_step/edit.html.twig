{% extends 'layout.html.twig' %}

{% block title %}Modification d'une étape d'intégration{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header text-center">
                        <h1>Modifier une étape d'intégration</h1>
                    </div>
                    <div class="m-3">
                        {{ include('integration_step/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                        {{ include('integration_step/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
        <div class="d-flex justify-content-center mt-3 mb-5">
            <a class="btn btn-primary" href="{{ path('integration_step_index') }}">Retour</a>
        </div>
    </div>

{% endblock %}
