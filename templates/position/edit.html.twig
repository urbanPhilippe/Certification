{% extends 'layout.html.twig' %}

{% block title %}Modifier une fonction{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h1 class="text-center">Modifier une fonction</h1>

                        {{ include('position/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

                        {{ include('position/_delete_form.html.twig') }}

                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-center mb-5">
        <a class="btn btn-primary" href="{{ path('position_index') }}">Retour</a>
    </div>
{% endblock %}
