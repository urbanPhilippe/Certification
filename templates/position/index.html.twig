{% extends 'layout.html.twig' %}

{% block title %}Gestion des fonctions{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12 mb-5">
                <div class="card w-50 m-auto">
                    <div class="card-header text-center">
                        <h1>Toutes les fonctions</h1>
                    </div>
                    <div class="d-flex justify-content-center mt-3">
                        <a href="{{ path('position_new') }}" class="btn btn-primary mb-3">Ajouter un métier</a>
                    </div>
                    <table class="table">
                        <thead>
                        <tr>
                            <th></th>
                            <th></th>
                            <th></th>
                            <th>Nom</th>
                            <th></th>
                            <th></th>
                            <th></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for position in positions %}
                            <tr>
                                <td></td>
                                <td></td>
                                <td></td>
                                <td>{{ position.name }}</td>
                                <td>
                                    <a href="{{ path('position_show', {'id': position.id}) }}">
                                        <i class="fas fa-search-plus mr-5"></i>
                                    </a>
                                    <a href="{{ path('position_edit', {'id': position.id}) }}">
                                        <i class="fas fa-pencil-alt"></i>
                                    </a>
                                </td>
                                <td></td>
                                <td></td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="4">Aucun enregistrement</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
