<div class="mt-3">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-2">{{ button_label|default('Envoyer le rendez-vous') }}</button>
    {{ form_end(form) }}
</div>
